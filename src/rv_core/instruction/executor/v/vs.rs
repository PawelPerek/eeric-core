use crate::rv_core::instruction::executor::prelude::*;

pub fn v(
    Vs { vs3, rs1, vm }: Vs,
    eew: SEW,
    x: &IntegerRegisters,
    v: &VectorRegisters,
    mem: &mut Memory,
) {
    todo!()
}
