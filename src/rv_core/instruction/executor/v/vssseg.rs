use crate::rv_core::instruction::executor::prelude::*;

pub fn v(
    Vss { vs3, rs1, rs2, vm }: Vss,
    eew: usize,
    nf: usize,
    v: &VectorRegisters,
    vec_engine: &VectorEngine,
    mem: &mut Memory,
) {
    todo!()
}
