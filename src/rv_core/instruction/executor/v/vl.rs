use crate::rv_core::instruction::executor::prelude::*;

pub fn v(Vl { vd, rs1, vm }: Vl, eew: usize, x: &IntegerRegisters, v: &mut VectorRegisters, mem: &Memory) {
//     let mut vec = Vec::with_capacity(eew);
    
//     for offset in 0 .. eew {
//         let addr = x[rs1] as usize + offset;

//         let byte = mem.get::<1>(addr);

//         vec.push(byte);
//     }

    todo!()
}
